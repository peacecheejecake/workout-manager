# M1-05i2a · 코치 실행 원장·API 경계

상태: **completed**. 선행 M1-05i1 완료.

근거 v2와 대화 revision, 현재 core-v2 의존성, AI 동의를 같은 DB 시점에 검사한 후
`queued` 실행 원장·멱등 receipt·ID만 담은 outbox를 한 transaction에 기록한다. 같은 키의
재전송은 기존 상태를 반환한다. 실행은 tenant/RLS 범위에서 조회·목록·취소할 수 있고,
근거 삭제 또는 동의 철회는 활성 실행을 취소하고 내부 미검증 출력을 제거한다.
종료 상태의 질문·실패 문구도 결과 종류를 유지한 채 일반 문구로 회수한다.

Fastify `/bff/v1` 경계는 경로·query·body·멱등 헤더를 엄격히 검사하고 기존 인증·세션·CSRF
검사를 거친다. `createConfiguredApi`에는 아직 원장을 연결하지 않는다. i2b의 runner 및
실행 직전/직후 재검사·운영 내보내기/복원이 준비되기 전 일반 서버에서 새 실행을 받을 수 없다.
이 작업에는 실제 모델 호출이나 검증된 Decision/Proposal이 없다.

검증: PostgreSQL integration 24파일·187개, API inject 4개, contracts 7개,
API·persistence typecheck, 변경 파일 ESLint·Prettier, DAG 참조·순환 검사.
Herdr 분할 pane 리뷰의 base/tree identity와 최종 결과는 task 커밋 본문에 기록한다.
