# M2-01t · S09 실내 기록 경로 탭: 사양을 구현에 맞춤

상태: **완료.** 사양 개정 + 실제 브라우저 증거 추가. 독립 검토 2라운드(tree `5481eec0`) 승인(차단 0). 구현 기준 HEAD `abea557`, 병합 때 `2669850` 위로 rebase했다. 제품 코드는 바꾸지 않았다.
`task-graph.json`·`AGENTS.md`·`CLAUDE.md`·`.geo-build`는 건드리지 않았다. 커밋과 peer review는 root 몫이다.

## 결정

- **사용자 결정(명시, 2026-09-23):** "M2-01t는 현재 구현에 맞게 스펙을 고쳐".
- 배경: M2-01k 매트릭스 `S09-indoor-tab`은 **failed**였다. 사양은 "실내·GPS 없는 기록은 경로 탭을
  설명과 함께 비활성화한다"였는데, 구현(M2-01e)은 경로 탭을 **활성**으로 두고 패널 안에서 위치가 없다고
  설명한다(`activity-detail-tabs.test.tsx:87`이 활성을 단언). 독립 검토자는 활성 탭 + 패널 설명이 비활성
  탭보다 설명에 접근하기 쉽다고 판단했다(M2-01k.md §8).
- 그래서 **동작은 그대로 두고 사양을 고쳤다.** 변경 기록은 [CHANGELOG](../../.pre/CHANGELOG.md)
  2026-09-23 항목에 남겼다.

## 사양 변경 (`docs/.pre/01_product_screen_spec.md:137`, §7.2 둘째 문단의 마지막 문장)

```diff
-… 실내·GPS 없는 기록은 경로 탭을 설명과 함께 비활성화한다.
+… 실내·GPS 없는 기록도 경로 탭은 비활성화하지 않는다. 탭 안에서 위치가 기록되지 않았음을 설명하고,
+지도를 그리지 않은 채 요약을 보여 주며, 없는 위치로 경로를 만들어 내지 않는다.
```

문단의 나머지(구간 강조, shared selection store, GPS 끊김 점선/갭)는 그대로다.

**문언을 고른 이유(구현 대조):**

| 문언                                  | 구현                                                                                                                                           |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 경로 탭은 비활성화하지 않는다         | `activity-detail-tabs.tsx:16` 활성 목록에 `route`                                                                                              |
| 위치가 기록되지 않았음을 설명         | `activity-track-panel.tsx:234-238` `positionedSampleCount === 0` → "위치가 기록되지 않은 활동입니다. 지도는 표시하지 않고 요약만 보여 줍니다." |
| 지도를 그리지 않는다                  | `activity-track-panel.tsx:639` 그릴 정점이 0이면 `MapLeaf`를 마운트하지 않고 "그릴 좌표가 없습니다"                                            |
| 요약을 보여 준다                      | 같은 패널의 "기본 요약"(경과 시간·기기 보고 거리·페이스·심박), 상단 활동 요약                                                                  |
| 없는 위치로 경로를 만들어 내지 않는다 | 서버 `map_path`의 좌표가 빈 배열이고 `insufficient: no-position`만 남는다                                                                      |

"요약**만**"이라고 쓰지 않은 이유: 관측 상세가 있으면 같은 패널에 **관측 그래프**도 보인다(지도 대신
보이는 것이 요약 하나뿐인 것은 아니다). 패널 안 문구 "요약만 보여 줍니다"는 지도 대신이라는 뜻으로 읽히며,
제품 동작 변경 금지에 따라 그대로 두었다.

**같은 규칙을 다시 적은 다른 문서:** `docs/.pre`·`docs/implementation`에서 찾았다. 현행 사양·수용 목록·
요구 색인(05)에는 없었다. `M1-04au.md:12`("경로·미디어 탭은 제공 불가 사유와 함께 비활성화")와
`M2-01e.md:187`, `M2-01k.md`의 해당 서술은 **당시 기록**이라 보존했다. `task-graph.json`의 M2-01t 항목은
root 소유라 건드리지 않았다.

## 증거

### 새 E2E — `tests/identity/activity-track-no-position.spec.ts`

- **fixture:** 합성 FIT(`packages/track-parsing/tests/fit-fixture`). record 6개에 시각·거리·심박만 있고
  **위치가 전혀 없다**(`longitude/latitude: null`). 개인 FIT/GPS는 쓰지 않았다.
- **경로:** 실제 OIDC fixture 로그인 → `/bff/v1/activity-imports` → track 업로드 예약·PUT·finalize
  (서버가 bounded worker에서 다시 파싱) → 전체 페이지 로드. `activity-track-map.spec.ts`와 같은 방식이다.
- **두 shell:** Next(3100)와 Vite(4200) 각각 한 시험.
- **단언:**
  1. 서버 `map_path`: 좌표·정점 표본 id가 빈 배열(경로를 만들어 내지 않음).
  2. `detailTab=overview`에서 시작해 `경로` 탭이 **enabled**이고 선택되지 않은 상태 → 클릭 →
     `aria-selected=true`, URL `detailTab=route`.
  3. 패널 "전체 6개 · 위치 있음 0개", 설명 문구 전체 일치.
  4. 지도 pane(`저장된 경로 지도` group): "그릴 좌표가 없습니다"가 보이면(기하 조회가 끝났고, 지도 leaf가
     마운트된다면 같은 commit에 들어온다) pane의 자식은 **그 안내 하나뿐**이고 지도 로딩 fallback이 없다.
     `networkidle` 뒤에도 자식은 하나이고, 지도 region `저장된 활동 경로`·`canvas`·"렌더된 경로 feature"·
     "지도가 경로를 그렸습니다"가 0개, `시작 지점`·`끝 지점` 비활성.
  5. 패널 "기본 요약"(50초, 640m)과 상단 `활동 요약 출처` region 표시. page error 0.

### 변이 (단일 anchor, 모두 원복 후 `cmp` 동일 확인, 매번 Next·Vite 재빌드)

독립 검토(tree `ebfe8f99`)의 B1: 이전 판의 지도 음성 단언은 설명 문구 직후, 기하 조회와 lazy MapView chunk가
끝나기 전에 돌아 **지도가 실제로 마운트돼도 통과했다**(검토자 변이 M2b). 그래서 지도 pane의 안내를 먼저
기다리고, pane이 자식 하나(안내)만 가지는지 구조로 확인하고, `networkidle` 뒤 음성 단언을 다시 보도록 고쳤다.
중복이던 "그릴 좌표가 없습니다" 단언(N2)은 pane 단언으로 합쳤다.

최종 spec(sha256 `858e38f3…931fe7`)으로 돌린 결과:

| 변이                                                                                      | 결과                                                                                                     |
| ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| M1 `activity-track-panel.tsx:234` `positionedSampleCount === 0` → `< 0` (설명 제거)       | 두 shell 모두 FAIL, spec:173 설명 문구 `element(s) not found`                                            |
| M2 `activity-track-panel.tsx:639` `total > 0` → `total >= 0` (안내 대신 지도 leaf 마운트) | 두 shell 모두 FAIL, spec:179 pane의 "그릴 좌표가 없습니다" `element(s) not found`                        |
| M2b 같은 줄 앞에 안내를 무조건 넣고 `total >= 0` (지도 leaf 마운트 **+** 안내 유지)       | 두 shell 모두 FAIL, spec:180 pane 자식 수 `toHaveCount(1)` — `Received: 4`                               |
| M2b + spec에서 자식 수 단언 두 줄 삭제 (음성 단언만 남김)                                 | 두 shell 모두 FAIL, spec:184 `저장된 활동 경로` region `toHaveCount(0)` — `Received: 1` (networkidle 뒤) |
| M3 `activity-detail-tabs.tsx:16` 활성 목록에서 `'route'` 제거 (경로 탭 비활성)            | 두 shell 모두 FAIL, spec:161 `toBeEnabled` — `Received: disabled`                                        |

M2b 두 번째 줄은 spec 파일도 변이했으며 제품 파일과 함께 원복해 둘 다 `cmp` 동일을 확인했다. 원복 뒤 반복
3회는 두 shell 6/6 통과.

**이 시험이 증명하는 것과 한계:** 기하 조회가 끝난 시점에 지도 pane은 안내 하나만 가진다(지도 leaf가
마운트되면 로딩 fallback·실패 fallback·renderer section 중 하나가 같은 commit에 pane 자식으로 들어오므로 여기서
잡힌다). 네트워크가 잠잠해진 뒤에도 renderer region·canvas·그린 경로가 없다. 음성 단언(region 0개)도 M2b에서
스스로 실패함을 확인했다. 증명하지 않는 것: `networkidle` 뒤에야 지도를 마운트하는 경우, 그리고 픽셀(그린 것이
없으므로 볼 픽셀도 없다). `networkidle`은 단독 신호로 쓰지 않았고, 안내 표시와 pane 구조를 먼저 기다린다.

이전 판(sha256 `54962fff…`, `d3f942db…`)의 M1·M2·M3 결과와 `pnpm test:identity` 2회는 검토 전 기록으로 남긴다
(아래 결과 표).

### 실제 브라우저 순서 (AGENTS.md: Aside → Chrome → Playwright)

- **Aside**(CLI 1.26.916.1741, `aside repl`): identity harness를 lock 아래 수동으로 띄우고(API 4300, Next
  3100, Vite 4200) fixture로 로그인, 같은 origin API로 위치 없는 FIT 활동을 만들었다. **3100과 4200 모두**에서
  `개요`로 연 뒤 `경로` 탭을 클릭 → tab `[selected]`, 패널에 "일부 데이터 위치가 기록되지 않은 활동입니다…",
  지도 group에 "기록 없음 그릴 좌표가 없습니다…", 기본 요약(50초·640m·133bpm), 표본 목록 "그려진 표본이
  없습니다.", `시작 지점`·`끝 지점`·`전체 보기` disabled, **canvas 0**(패널·페이지 모두).
  - 한계: Aside의 `page.url()`은 탭 클릭 뒤에도 `detailTab=overview`를 보고했다(클라이언트 URL 갱신을 따라가지
    않는 것으로 보임). URL 갱신은 Playwright spec이 단언한다. 전체 페이지 스크린샷은 Aside에서 같은 영역이
    반복되어 찍혀 판독용으로 쓰지 않았고, 판단은 접근성 트리로 했다. 첫 시도에서 finalize가 400이었는데
    (빈 본문에 `content-type: application/json`을 붙인 스크립트 실수), 헤더를 고친 뒤 성공했다.
- **Chrome:** 이 세션에는 Chrome을 조작할 도구가 없어 쓰지 않았다.
- **Playwright:** 위 spec(Chromium headless).

### 단위 시험(기존, 변경 없음)

- `activity-detail-tabs.test.tsx:87` "enables the route tab and renders its panel content"
- `activity-track-panel.test.tsx:262` "reports a recording with no fix and still shows the summary"(API mock)

## 매트릭스

`docs/implementation/research/m2-01k-requirement-matrix.json`의 `S09-indoor-tab`:

- verdict **failed → passed**. E2E가 두 shell에서 실제로 실행되어 통과했기 때문이다(아래 표).
- quote를 새 사양 문장으로 바꾸고, evidence에 새 E2E 두 시험 이름·변이 결과·기존 단위 시험 둘을 적었다.
  reason에 사용자 결정·이전 문장·위 한계를 적었다.
- counts: passed 27 → **28**, failed 2 → **1**(합계 110 불변, 행 집계와 일치 확인).

## 결과(실측, Node 24.12.0 / pnpm 10.34.5)

| 검사                                                               | 결과                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `pnpm check:generated` · `pnpm lint` · `pnpm typecheck`            | 통과 · 통과 · 통과(34/34)                                                                                                                                                                                                            |
| `pnpm build`                                                       | 15/15(변이 때마다 web·mobile-web 4/4 재빌드, 원복 뒤 재빌드)                                                                                                                                                                         |
| 새 spec 첫 실행                                                    | **2 passed**(Next·Vite)                                                                                                                                                                                                              |
| 새 spec `--repeat-each=3`                                          | **6 passed** ×4회(검토 전 판 3회, 검토 반영 최종 판 `858e38f3…` 1회) — 매회 shell마다 3회                                                                                                                                            |
| 변이 M1·M2·M2b·M2b(음성만)·M3(최종 판)                             | 각각 2 failed(두 shell), 원복 `cmp` 동일, 원복 뒤 6 passed                                                                                                                                                                           |
| `pnpm test:identity` 1회차(load 27→65)                             | 161 passed / **1 failed** / 2 skipped. 실패 `coaching-runs.spec.ts:546`(코칭 승인 UI, `toBeVisible` element not found) — 이 변경과 무관, 2회차에서 통과                                                                              |
| `pnpm test:identity` 2회차(load 43→47)                             | **162 passed / 2 skipped**(skip 2 = fixture 모드의 인증 OP spec). 새 spec 2건 포함                                                                                                                                                   |
| `pnpm test` 1회차(load 25→38)                                      | 3,301 passed / 9 failed(5 files). 8건 5 s timeout + `activity-workbench` 20k "499 기대, 500" 1건. 그 5 파일만 재실행 시 load 38→63에서 여전히 6건 timeout                                                                            |
| `pnpm test` 2회차(load 46→39)                                      | 3,306 passed / 4 failed: timeout 3건(`data-panels`, `activity-workbench` 2) + `packages/server/media/tests/tenant-listing.test.ts` "never answers a listing through a root swapped during the walk"(경합 시험, 1/161 링크 경유 응답) |
| 위 3 파일 단독 재실행(load 36)                                     | **20/20 passed**                                                                                                                                                                                                                     |
| 검토 반영 후 `pnpm test:identity` 1회(최종 spec, lock, load 27→42) | **162 passed / 2 skipped**, 실패 0                                                                                                                                                                                                   |
| `pnpm format:check`                                                | 통과(마지막 실행)                                                                                                                                                                                                                    |

`pnpm test`는 이 노드가 바꾼 파일을 하나도 포함하지 않는다(vitest는 `tests/identity`를 읽지 않고, 제품 코드는
바뀌지 않았다). 실패는 모두 부하 중 timeout 또는 다른 노드(M2-01x)의 경합 시험이다. 1회차의 planning
3 파일은 2회차 전체 실행에서 통과했고, 2회차의 3 파일은 단독 재실행에서 통과했다.
**다만 `tenant-listing` 경합 실패는 부하 때문이라고 단정하지 않는다** — 경합 창에서 링크를 통해 한 번 답했다는
관측이므로 M2-01x 담당이 확인할 가치가 있다.

## 병합 검증(root, `2669850` 위로 rebase, load 약 12–19)

시험 파일·문서·매트릭스만 바뀌므로 unit·integration·drill은 돌리지 않았다(제품 코드와 DB 변경 없음).

- `install --frozen-lockfile`, `check:generated`, `lint`, `typecheck`: 통과. `build` 15/15. 매트릭스·task-graph JSON 유효.
- `pnpm test:identity` 2회 모두 178 통과·3 skipped(`oidc-certified.spec.ts`, 실제 OP 필요, 미실행). 새
  `activity-track-no-position.spec.ts` 두 시험(Next·Vite) 포함.
- `format:check`: 통과(마지막).
