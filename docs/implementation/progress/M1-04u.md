# M1-04u · Period Explorer·Orbit 계층 탐색

상태: 구현·검증 완료. S04 §5.1 / V2-F08 / V2-A12의 기간 탐색.

## 범위

- 기존 계획 API와 검증된 기간 트리로 현재 부모·직접 자식·breadcrumb을 표시한다.
- 원형 각도는 현지 날짜 길이이며 부모 범위의 미배정 일자를 공백으로 보존한다.
  번호와 동일한 HTML 목록을 제공하고 hover/focus는 미리보기만, 클릭/Enter는 URL 탐색이다.
- 기간별 달력 전환은 조회 lens 변경이며 계획·실제 기록을 쓰지 않는다.
- 지연 로드한 원형은 도메인 독립 view props를 받는다. 목록은 원형 로딩과 독립적이다.
- 잘못된 초안·없는 기간·빈 기간·leaf를 구분하고 선택 이동 후 문맥 focus를 유지한다.
- 전체 S04 완료는 아니다. timeline·주요 대회/핵심 세션·우선순위/제약·완전 집계·변화 이력·
  기간 이동 시 자식 이동 확인·scenario branch 비교는 남는다.

## 검증

- `pnpm check`: 56 files / 702 tests 통과. production build 7 tasks 통과.
  순수 모델 12 tests와 UI 6 tests에서 윤년·DST·0001/0099/9999년·불균등 기간·
  미배정 날짜·부분 Block·없는 ID·leaf·빈 기간·원본 불변·키보드 동일 선택을 검증했다.
- 실제 OIDC·격리 PostgreSQL 전체 identity E2E에서 기존 42개와 Orbit 3개가 통과했다.
  남은 Orbit 1개의 새 hover와 오래된 미리보기 구분을 수정 후 Orbit 4개 모두 통과했다 (10.7초).
  원형 SVG arc 실측 40:1 비율, 작은 sector 번호와 목록 동일 선택, hover/focus URL 불변,
  reload/back/forward/breadcrumb, 320px·14개 viewport 왕복의 세션 선택·편집 초안 유지,
  명시적 calendar lens 전환, PUT 0회·서버 head·실제 기록 불변을 검증한다.
- 없는 기간 URL의 명시 복구, 잘못된 초안에 저장 계획을 대신 표시하지 않는 것,
  Alice→Bob→Alice 계정 전환의 초안 소거를 실제 브라우저로 확인했다.
- 실제 원형 JS chunk를 차단해 오류 안내·목록 탐색 유지·명시 재시도 복구를 확인했다.
  1회 차단은 browser 자동 재시도로 바로 회복되어, 실패 검증에서는 사용자 재시도 전까지 차단을 유지했다.
- 초기 E2E의 작은 sector 번호 없음 기대값은 요구와 달라 번호 2 표시로 수정했다.
- Aside 실제 production 화면에서 Season→Wave→Phase Enter 탐색·문맥 focus,
  10일/1일 Block·미배정 9일, 목록 focus 미리보기·부분 Block 선택·breadcrumb 복귀를 확인했다.
  캡처를 직접 열어 확인했다:
  `/Users/min.jiwon/.aside/u/0/sessions/2026-09-17_fLi3V1nqgSEQaBxY/artifacts/period-orbit.png`.
- Aside에서 breadcrumb 복귀 시 이전 미리보기가 다시 나타나는 문제를 발견했다.
  preview 수명을 현재 선택에 묶어 변경 시 초기화하고, unit 및 실제 back 탐색 회귀를 추가했다.
- 최종 E2E에서 목록 mouseleave가 원형 focus 미리보기를 지우는 경합을 발견했다.
  hover/focus ID를 분리하고 focus를 우선하도록 수정해 별도 회귀 테스트를 추가했다.
- 뒤로 탐색 검증에서는 포인터가 재등장한 항목 위에 있으면 새 hover가 발생한다.
  오래된 미리보기 복원 검증을 위해 포인터를 항목 밖으로 옮긴 뒤 back·문맥 focus를 확인했다.
- 이번 task는 UI/조회 탐색 변경이다. 새 DB 계약·migration·integration 재실행은 없다.
  지도·실기기·OS IME·공식 Garmin 검증 gate와 규칙 파일을 변경하지 않았다.
- graph DAG·완료 의존성·evidence 경로를 확인하고, Herdr 커밋 전 독립 리뷰의
  정확한 base/tree 및 결과는 커밋 메시지에 기록한다.
