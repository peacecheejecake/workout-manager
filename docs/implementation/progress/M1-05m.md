# M1-05m · 러닝 코치·승인 통합 수용

상태: **completed** (2026-09-18). [수용 E2E](../../../tests/identity/coaching-acceptance.spec.ts)는
로컬 OIDC 로그인 후 합성 수동 활동을 실제 BFF·격리 PostgreSQL에 저장한다. 활동
상세와 3일 대시보드에서 조회한 뒤, 저장 근거 snapshot의 활동 ID·revision·수동
출처·거리/시간·사용자 RPE 및 dependency count를 확인한다. S10에서 명시적으로
결정론적 비프로덕션 worker를 실행하고 서버 검증 후보를 만든다. S11은 1800→2100초
diff, 전후 일정, 예상 영향, 미확인 0건을 보여준다. 확인 전 승인 버튼은 비활성이고
승인 전 계획 head는 불변이다. 사용자가 체크하고 승인한 뒤 실제 저장 head가 정확히
한 버전 증가하고 새 history 항목을 가리키는지 재조회한다.

기존 실DB 회귀 근거를 함께 대조했다.

| 경계        | 검증 근거                                                                                                                                                                           |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 철회        | `coaching-candidates.integration.test.ts`의 생성↔철회 경합, sealed body/digest 제거, 승인 후 철회·재전송·비식별 export; `coaching-runs.spec.ts`의 철회 후 404/목록 제외             |
| 계정 삭제   | `operations.integration.test.ts`의 철회·export 경합과 coaching 테이블 삭제; `coaching-runs.integration.test.ts`의 삭제 후 부활 차단                                                 |
| worker 실패 | `coaching-runs.integration.test.ts`의 재시도 한도, throw/잘못된 형식/과대 출력 시 terminal·출력 미저장, outbox ack rollback, 실행 중 철회/취소; `coaching-runs.spec.ts`의 취소 유지 |
| 동시 승인   | `coaching-candidates.integration.test.ts`의 동일 키 replay, 서로 다른 키 중 하나만 성공, outbox 충돌 시 전체 rollback; `coaching-runs.spec.ts`의 승인·재시도·새 버전                |

검증: 새 수용 테스트와 기존 코칭 OIDC 테스트 **7/7**, 격리 PostgreSQL 통합
**226/226**, 생성물 검사·Prettier·ESLint·전체 TypeScript 통과. 테스트는 로컬
OIDC와 `synthetic-v1` worker fixture를 사용한다. 실제 LLM 판단·공식 Garmin
수집·생리학적 효과·외부 공개 gate의 완료 증거는 아니다.

Herdr split-pane 리뷰의 base/tree identity와 결과는 task 커밋 본문에 기록한다.
