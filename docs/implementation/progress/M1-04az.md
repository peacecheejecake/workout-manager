# M1-04az · 계획 종류 도메인 열

상태: **completed**. 선행 M1-04q 완료.

S05의 `계획 종류`는 운동·영양·회복·루틴 등 도메인을 뜻한다. 운동 종목인 `sport`와
구분한다. 현재 Planner의 행은 운동 세션만 담으므로 `운동`으로 표시한다. 영양·회복·루틴의
계획 행은 각 도메인 통합 작업에서 추가한다. 현재 운동 세션 계약에 다른 도메인 값을 저장하지
않는다.

표의 열 표시·고정·URL 복원에 `planType` 열을 추가했다. 관련 표·URL·시나리오·API 계약
검사 50개, 전체 단위 검사 1,702개, PostgreSQL 통합 검사 181개, typecheck 18개 task,
build 7개 task, lint 및 Prettier가 통과했다. 계획 표 E2E도 통과했다. Aside 실제 브라우저에서
저장한 운동 세션의 `운동` 값과 열 표시·고정 조작부를 확인했다.
Herdr 독립 리뷰에서 추가 actionable finding은 없었다. 새 열의 고정 상태를 reload한 뒤
복원하는 별도 assertion은 없으며, 기존 공통 열 처리 경로를 사용한다.
